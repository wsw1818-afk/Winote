# UX/UI 개선 제안서

## 개요
Winote 앱의 사용자 경험(UX)과 사용자 인터페이스(UI) 분석 결과

---

## 🎯 주요 개선 권장사항

### 1. 접근성 (Accessibility) 개선

#### 현재 상태
- 일부 아이콘 버튼에 Tooltip 적용됨 ✅
- 시맨틱 라벨이 일부 누락됨 ⚠️

#### 개선 제안
```dart
// 개선 전 (accessibility label 부재)
IconButton(
  icon: const Icon(Icons.undo),
  onPressed: () {},
)

// 개선 후 (의미 있는 라벨 추가)
IconButton(
  icon: const Icon(Icons.undo),
  onPressed: () {},
  tooltip: '실행취소 (Ctrl+Z)',  // 단축키 정보 포함
)
```

**적용 위치**:
- `quick_toolbar.dart`: 모든 툴 버튼
- `editor_page.dart`: 페이지 네비게이션 버튼
- `library_page.dart`: 노트 카드

**우선순위**: 중간

---

### 2. 저장 상태 표시 (Save Status Indicator)

#### 현재 상태
- 저장 필요 여부는 `_hasChanges`로 관리됨
- "저장 중" 로딩 표시 없음 ⚠️
- 저장 완료 피드백은 있음 ✅

#### 개선 제안
```dart
// EditorPage AppBar에 저장 상태 인디케이터 추가
AppBar(
  title: Row(
    children: [
      Text(_currentNote?.title ?? '새 노트'),
      if (_isSaving) ...[
        const SizedBox(width: 8),
        const SizedBox(
          width: 12,
          height: 12,
          child: CircularProgressIndicator(strokeWidth: 2),
        ),
      ],
    ],
  ),
)
```

**효과**:
- 자동 저장 중 사용자가 인지 가능
- 저장 완료 전 앱 종료 방지

**우선순위**: 높음

---

### 3. 페이지 전환 애니메이션

#### 현재 상태
- 페이지 이동 시 즉시 전환 (애니메이션 없음)
- 사용자가 페이지가 바뀌었는지 인지하기 어려움

#### 개선 제안
```dart
// 페이지 전환 시 부드러운 애니메이션
AnimatedSwitcher(
  duration: const Duration(milliseconds: 200),
  transitionBuilder: (child, animation) {
    return FadeTransition(
      opacity: animation,
      child: SlideTransition(
        position: Tween<Offset>(
          begin: const Offset(0.05, 0),
          end: Offset.zero,
        ).animate(animation),
        child: child,
      ),
    );
  },
  child: KeyedSubtree(
    key: ValueKey<int>(_currentPageIndex),
    child: DrawingCanvas(...),
  ),
)
```

**효과**:
- 페이지 전환 시각적 피드백 제공
- 전환 중간 상태 명확화

**우선순위**: 중간

---

### 4. 터치 타겟 크기 최적화

#### 현재 상태
- 툴 버튼 크기: 36x36 (Material 기본)
- 태블릿/터치 환경에서는 작을 수 있음

#### 개선 제안
```dart
// quick_toolbar.dart
Widget _buildIconButton(...) {
  return Tooltip(
    message: tooltip,
    child: InkWell(
      onTap: onTap,
      borderRadius: BorderRadius.circular(8), // 더 큰 터치 영역
      child: Container(
        padding: const EdgeInsets.all(12), // 8 → 12로 증가
        constraints: const BoxConstraints(
          minWidth: 48, // 최소 터치 크기
          minHeight: 48,
        ),
        child: Icon(icon, size: 20),
      ),
    ),
  );
}
```

**WCAG 가이드라인**: 최소 44x44px 터치 타겟 권장

**우선순위**: 높음 (태블릿 사용성)

---

### 5. 키보드 단축키 안내

#### 현재 상태
- 단축키 기능은 구현됨 ✅ (`editor_page.dart` Shortcuts)
- UI에서 단축키 정보 표시 안됨 ❌

#### 개선 제안
**옵션 A**: 툴팁에 단축키 표시
```dart
_buildIconButton(
  Icons.undo,
  widget.onUndo,
  tooltip: '실행취소 (Ctrl+Z)', // 단축키 포함
)
```

**옵션 B**: 단축키 도움말 다이얼로그
- 설정 또는 메뉴에 "키보드 단축키" 항목 추가
- 모든 단축키 목록 표시

**우선순위**: 낮음

---

### 6. 빈 상태 (Empty State) 개선

#### 현재 상태
- HomePage: 구현됨 ✅
- LibraryPage: 구현됨 ✅
- 검색 결과 없음: 구현됨 ✅

#### 개선 제안 - 온볇ィング 추가
```dart
// 첫 실행 시 튜토리얼 오버레이
if (_isFirstRun) {
  return Stack(
    children: [
      _buildContent(),
      TutorialOverlay(
        steps: [
          TutorialStep(
            target: _fabKey,
            title: '새 노트 만들기',
            description: '버튼을 눌러 첫 노트를 만들어보세요!',
          ),
        ],
      ),
    ],
  );
}
```

**우선순위**: 낮음

---

### 7. 토스트/스낵바 개선

#### 현재 상태
- `FeedbackService`로 일관된 피드백 ✅
- Undo 액션 포함된 메시지 ✅

#### 개선 제안 - 햅틱 피드백 추가
```dart
// feedback_service.dart
import 'package:flutter/services.dart';

void showSuccess(...) {
  // 성공 시 가벼운 진동
  HapticFeedback.lightImpact();
  // ... existing code
}

void showError(...) {
  // 오류 시 강한 진동
  HapticFeedback.heavyImpact();
  // ... existing code
}
```

**효과**:
- 시각적 피드백 + 각적 피드백
- 접근성 향상

**우선순위**: 중간

---

### 8. 제스처 힌트 제공

#### 현재 상태
- 두 손가락/세 손가락 제스처 구현됨
- 사용자가 제스처를 모를 수 있음

#### 개선 제안
```dart
// 첫 사용 시 제스처 힌트 표시
class GestureHintOverlay extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        _buildHint(
          icon: Icons.touch_app,
          text: '두 손가락으로 확대/축소',
        ),
        _buildHint(
          icon: Icons.undo,
          text: '두 손가락 탭: 실행취소',
        ),
        _buildHint(
          icon: Icons.redo,
          text: '세 손가락 탭: 다시실행',
        ),
      ],
    );
  }
}
```

**우선순위**: 중간

---

### 9. 다크 모드 일관성

#### 현재 상태
- 앱 테마: 다크 모드 지원 ✅
- 캔버스: `_darkCanvasModeEnabled` 별도 관리 ⚠️

#### 개선 제안
- 캔버스 다크 모드를 시스템 테마와 동기화 옵션 제공
```dart
// 설정에 추가
SwitchListTile(
  title: const Text('캔버스를 시스템 테마와 동기화'),
  subtitle: const Text('앱 다크 모드 변경 시 캔버스도 함께 변경'),
  value: _syncCanvasWithSystem,
  onChanged: (value) {
    // 시스템 테마 변경 리스너 등록
  },
)
```

**우선순위**: 낮음

---

### 10. 검색 UX 개선

#### 현재 상태
- 실시간 검색 필터링 ✅
- 검색 결과 없음 표시 ✅
- 검색어 하이라이트 ❌

#### 개선 제안
```dart
// 검색 결과에서 검색어 하이라이트
Widget _buildHighlightedTitle(String title, String query) {
  if (query.isEmpty) return Text(title);
  
  final parts = title.split(query);
  return RichText(
    text: TextSpan(
      children: [
        for (int i = 0; i < parts.length; i++) ...[
          TextSpan(text: parts[i]),
          if (i < parts.length - 1)
            TextSpan(
              text: query,
              style: const TextStyle(
                backgroundColor: Colors.yellow,
                fontWeight: FontWeight.bold,
              ),
            ),
        ],
      ],
    ),
  );
}
```

**우선순위**: 중간

---

## 📊 개선 우선순위 매트릭스

| 개선사항 | 사용자 영향 | 구현 난이도 | 우선순위 |
|---------|------------|------------|---------|
| 저장 상태 표시 | 높음 | 낮음 | 🔴 높음 |
| 터치 타겟 크기 | 높음 | 낮음 | 🔴 높음 |
| 접근성 라벨 | 중간 | 낮음 | 🟡 중간 |
| 페이지 전환 애니메이션 | 중간 | 낮음 | 🟡 중간 |
| 햅틱 피드백 | 중간 | 낮음 | 🟡 중간 |
| 검색 하이라이트 | 중간 | 중간 | 🟡 중간 |
| 제스처 힌트 | 중간 | 중간 | 🟡 중간 |
| 키보드 단축키 안내 | 낮음 | 낮음 | 🟢 낮음 |
| 온보딩 | 낮음 | 높음 | 🟢 낮음 |
| 캔버스 테마 동기화 | 낮음 | 낮음 | 🟢 낮음 |

---

## 🎨 UI 디자인 제안

### 툴바 개선
- **현재**: 10+ 개 아이콘이 한 줄에 배치
- **제안**: 
  - 주요 도구만 표시 (펜, 지우개, 올가미)
  - 나머지는 "더보기" 메뉴로 이동
  - 자주 사용하는 도구는 컨텍스트에 따라 동적으로 표시

### 색상 선택기
- **현재**: 팝업 메뉴로 색상 선택
- **제안**:
  - 최근 사용 색상 먼저 표시
  - 색상 프리셋 커스터마이징 기능

---

## ✅ 체크리스트

- [ ] 저장 상태 인디케이터 추가
- [ ] 터치 타겟 48x48px로 확대
- [ ] 접근성 라벨 전체 적용
- [ ] 페이지 전환 애니메이션
- [ ] 햅틱 피드백 추가
- [ ] 검색 결과 하이라이트
- [ ] 제스처 힌트 오버레이
- [ ] 단축키 안내 페이지
